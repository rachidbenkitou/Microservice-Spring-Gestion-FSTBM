eureka:
  client:
    register-with-eureka: false
server:
  port: 8222
spring:
  application:
    name: GATEWAY-SERVER
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: n1
          uri: http://localhost:8089
          predicates:
            - Path=/api/modules/**





